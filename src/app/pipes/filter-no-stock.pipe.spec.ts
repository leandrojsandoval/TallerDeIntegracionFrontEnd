import { FilterNoStockPipe } from './filter-no-stock.pipe';

describe('FilterNoStockPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterNoStockPipe();
    expect(pipe).toBeTruthy();
  });
});
